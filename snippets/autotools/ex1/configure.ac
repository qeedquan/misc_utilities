# Generates a configure script, when ran it will print the notice message
dnl is an m4 macro that discards all input that follows it on the same line, another way to do comments
AC_INIT(myconfig, version-0.1)
AC_MSG_NOTICE([Hello, world.])

