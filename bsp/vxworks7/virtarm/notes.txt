*** Overview
BSP for QEMU Virt ARM

qemu-system-arm -cpu cortex-a15 -machine virt -m 2G -serial stdio -display none -kernel vxWorks

*** CPU
Cortex-A15
Some instructions are generated by the compiler that QEMU doesn't support.
Use an older model (armarch7) to mitigate.
